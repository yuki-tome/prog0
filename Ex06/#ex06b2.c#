#include <stdio.h>
int main ( )
{
  int sum,i,j,k ;
  int kai,bunsi,aaa,n,r;

  kai = 1;
  bunsi = 1;
  aaa = 1;

  printf("nとrを入力してください：");
  scanf("%d%d",&n,&r);

  for(i=n;i>0;i--){
    
      kai = kai * i ;
    }
  for(j=r;j>0;j--){

    bunsi = bunsi * j ;
  }
  for(k=n-r;k>0;k--){

    aaa = aaa * k ;
  }

  sum = kai / bunsi / aaa;

  printf("%dC%d = %d\n",n,r,sum);

  return 0;
}
