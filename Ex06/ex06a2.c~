#include <stdio.h>
 
int main()
{
  int n, kai = 1;
  int i;
 
  printf("階乗を計算したい数を入力してください：");
  scanf("%d", &n);

  if( n>13 && n < 0 ){printf("計算できません");}
  else {

  for(i=n;i>0;i--){
    
      kai = kai * i ;
    }
  }

  printf("%d\n",kai);

  return 0;
}
